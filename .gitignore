node_modules/
.env
client/dist/
*.log
.DS_Store

# Cowork device-bridge mount forbids unlink, so git lock files and leftover
# temp objects get renamed here instead of deleted. Safe to remove from Finder.
_to_delete/
